# Copy to .env and fill in real values, then run: make k8s-secret
# .env is gitignored - never commit it.

MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=rkeplin
MYSQL_USER=changeme
MYSQL_PASSWORD=changeme
